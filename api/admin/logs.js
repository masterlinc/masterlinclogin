// ============================================================================
// api/admin/logs.js — 行为日志（前端兼容路径 /api/admin/logs）
// 复用 /api/admin/events 的实现（同一 handler，GET，支持 ?ev=&page=）
// ============================================================================
module.exports = require('./events.js');
